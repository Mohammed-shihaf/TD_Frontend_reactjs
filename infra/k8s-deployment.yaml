apiVersion: apps/v1
kind: Deployment
metadata:
  name: td-frontend-reactjs
  labels:
    app: td-frontend-reactjs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: td-frontend-reactjs
  template:
    metadata:
      labels:
        app: td-frontend-reactjs
    spec:
      containers:
        - name: app
          image: td-frontend-reactjs:20
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: td-frontend-reactjs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: td-frontend-reactjs
