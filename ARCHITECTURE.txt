PROJECT TYPE: Micro-Frontend Platform (standalone frontend module)
(extracted from the connected TD_Microfrontend_BE_nodejs_FE_reactjs_FE_angularjs repo)

BUILD COMMANDS
==============
npm install
npm test        # real mocha test suite, verified passing
npm run build    # real vite build, verified passing (React 20)

ARCHITECTURE
============
This is the React shell of the Micro-Frontend Platform, standalone.
In the connected repo it embeds the Angular remote live via
<iframe src="/angular-remote/">; that Angular build isn't present in
this single-technology repo (see TD_Frontend_angularjs's
microfrontend_angularjs<version> branches for the remote itself).

  src/widgets.js      plain module, real logic (also tool-fixture target)
  src/App.jsx          React UI consuming widgets.js
  test/widgets.test.js mocha + chai test suite
  quality/              13 real JS code-quality tool fixtures

Branches: microfrontend_reactjs<18|20|22|24>
