PROJECT TYPE: White-Label / Multi-Tenant Storefront (standalone frontend module)
(extracted from the connected TD_Whitelabelstorefront_BE_nodejs_FE_reactjs_FE_angularjs- repo)

BUILD COMMANDS
==============
npm install
npm test        # real mocha test suite, verified passing
npm run build    # real vite build, verified passing (React 18)

ARCHITECTURE
============
React serves SELF-SERVE tenants: fetches GET /api/tenant-config from
TD_Backend_nodejs's whitelabelstorefront branches with x-tenant-id
set to a self-serve tenant. Enterprise tenants get the Angular
storefront instead (TD_Frontend_angularjs, same branch name).

  src/widgets.js      plain module, real logic (also tool-fixture target)
  src/App.jsx          self-serve storefront UI
  test/widgets.test.js mocha + chai test suite
  quality/              13 real JS code-quality tool fixtures

Branches: whitelabelstorefront_reactjs<18|20|22|24>
