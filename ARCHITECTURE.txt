PROJECT TYPE: Framework Migration Bridge (standalone frontend module)
(extracted from the connected TD_Migrationbridge_BE_nodejs_FE_reactjs_FE_angularjs repo)

BUILD COMMANDS
==============
npm install
npm test        # real mocha test suite, verified passing
npm run build    # real vite build, verified passing (React 24)

ARCHITECTURE
============
This is the NEW app in the Migration Bridge: served at /app by
TD_Backend_nodejs's migrationbridge branches' route-split backend,
alongside the legacy Angular app at /legacy (TD_Frontend_angularjs,
same branch name).

  src/widgets.js      plain module, real logic (also tool-fixture target)
  src/App.jsx          new-app UI, fetches /api/widgets
  test/widgets.test.js mocha + chai test suite
  quality/              13 real JS code-quality tool fixtures

Branches: migrationbridge_reactjs<18|20|22|24>
