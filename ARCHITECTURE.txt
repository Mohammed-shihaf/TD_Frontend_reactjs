PROJECT TYPE: Multi-Product SaaS Platform (standalone frontend module)
(extracted from the connected TD_Saasplatform_BE_nodejs_FE_reactjs_FE_angularjs- repo)

BUILD COMMANDS
==============
npm install
npm test        # real mocha test suite, verified passing
npm run build    # real vite build, verified passing (React 20)

ARCHITECTURE
============
React only calls the /api/customer/* namespace of the SaaS Platform
backend (TD_Backend_nodejs's saasplatform branches). Angular
(TD_Frontend_angularjs, same branch name) only calls /api/admin/*.

  src/widgets.js      plain module, real logic (also tool-fixture target)
  src/App.jsx          customer-app UI
  test/widgets.test.js mocha + chai test suite
  quality/              13 real JS code-quality tool fixtures

Branches: saasplatform_reactjs<18|20|22|24>
