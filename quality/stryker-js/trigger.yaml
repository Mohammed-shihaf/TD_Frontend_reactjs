tool: stryker-js
language: javascript
language_version: "22"
technique: "Mutation Testing"
classification: "Mutation Score"
target_files: [../../src/widgets.js, ../../stryker.conf.json]
run:
  command: "npx stryker run"
expects:
  mutants_generated_and_tested: true
verified_in_session: true
verified_result: "real mutation run: 85.71% mutation score, 18 killed, 3 survived, 0 no-coverage — genuine survivors: unchecked label string literal, and the < vs <= boundary on classifyWidgetCount's small/medium thresholds"
